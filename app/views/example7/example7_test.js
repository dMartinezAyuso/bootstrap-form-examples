'use strict';

describe('myApp.example7 module', function() {

  beforeEach(module('myApp.example7'));

  describe('example7 controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var example7Ctrl = $controller('Example7Ctrl');
      expect(example7Ctrl).toBeDefined();
    }));

  });
});
