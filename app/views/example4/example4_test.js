'use strict';

describe('myApp.example4 module', function() {

  beforeEach(module('myApp.example4'));

  describe('example4 controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var example4Ctrl = $controller('Example4Ctrl');
      expect(example4Ctrl).toBeDefined();
    }));

  });
});
